using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HudScript : MonoBehaviour
{
/*    private Image hpBar;

    private float energyBar;

    private float bossBar;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //La valeur doit être comprise entre 0 et 1
    void setHp(float hpAmount)
    {
        hpBar.fillAmount = HpAmount;
    }

    void setEnergy(float energyAmount)
    {
        energyBar.fillAmount = EnergyAmount;
    }

    void setBoss(float bossAmount)
    {
        bossBar.fillAmount = bossBar;
    }*/
}
